.side_bar
  .side_contents
    .side_text
      .side_name
        = current_user.name
      .side_name_icon
        = link_to "#", class: "contents" do
          = icon('far', 'edit')
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog')
  .side_list
    .group 
      .group_name
        test
      .group_message
        test
    .group 
      .group_name
        test
      .group_message
        test