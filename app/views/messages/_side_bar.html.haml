.side_bar
  .side_contents
    .side_text
      .side_name
        = current_user.name
      .side_name_icon
        = link_to new_group_path do
          = icon('far', 'edit')
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog')
  .side_list
    - current_user.groups.each do |group|
      .group 
        = link_to '#' do
          .group_name
            = group.name
          .group_message
            メッセージはまだありません
        .group 
          .group_name
            test
          .group_message
            test